# Supabase project the PWA talks to. Copy to `.env` and fill in real values from
# the Supabase dashboard (Project Settings → API), or from `pnpm supabase start`
# for the local stack.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
